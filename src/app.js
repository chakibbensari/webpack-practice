import style from './main.css';
// This file uses to add the svg image into the dom by claling its function (component())
import component from './component.js';

console.log('¡Hola!');
console.log('adios');

document.body.append(component());
